{
  "devDependencies": {
    "parcel": "^2.0.0-beta.2",
    "typescript": "^4.2.3"
  },
  "dependencies": {
    "axios": "^0.21.1"
  },
  "scripts": {
    "start:db": "json-server -w db.json",
    "start:parcel": "npx parcel serve index.html"
  }
}
